version: "3.9"

services:
  annotation-api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5001:5001"
    volumes:
      - ./annotation_data:/app/annotation_data
    environment:
      - FLASK_ENV=production
